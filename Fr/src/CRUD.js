import React, {useState, useEffect} from "react";

const CRUD = () => {
    const [data, setData] = useEffect([]);

    const empdata = [
        {
            
        }

    ]

    return(
        <div>CRUD</div>
    )
}